<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%pageTitle%></title>
    <style>
        nav {
            width:100%;
            height: 4rem;
            background-color:rgb(147, 190, 255);
            padding: 0;
            display:flex;
            margin: auto;
            
            
            
            

        }
    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/add-input">Input Data</a></li>
            </ul>
        </nav> 
    </header>
    <% if(books.length > 0) { %>
        <% for (var i = 0; i < books.length; i++) { %>
            <p1> <%= books[i].name %>: </p1>
            <p2> <%= summaries[i].name %> </p2>
            <br>
        <% } %>

     
  
    <% } else { %>

        <h1>No Books listed</h1>
    <% } %>
</body>
</html>