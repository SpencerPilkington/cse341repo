<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../includes/header.ejs %>
</head>
<body>
    <% include ../includes/nav.ejs %>
    <% if(books.length > 0) { %>
        <% for (var i = 0; i < books.length; i++) { %>
            <p1> <%= books[i].name %>: </p1>
            <p2> <%= summaries[i].name %> </p2>
            <br>
        <% } %>

     
  
    <% } else { %>

        <h1>No Books listed</h1>
    <% } %>
</body>
</html>